export { 
    hashDid, 
    hashData, 
    generateCredentialId, 
    formatDid, 
    formatAddress, 
    formatBalance, 
    parseBalance, 
    formatTimestamp, 
    formatRelativeTime, 
    isCredentialExpired, 
    getExpiryStatus, 
    validateDidFormat, 
    generateDid 
} from '../substrate/utils';